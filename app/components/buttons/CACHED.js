import styles from "../global.module.css"

const CACHED = () => {
    return (
        <div className={`${styles.border_2} inline px-2 py-0.5`}>
            CACHED &gt;
        </div>
    )
}

export default CACHED;